# addis.world

built w/ preact ♪٩(✿′ᗜ‵✿)۶♪
```
.
├── art                   # /art page contents
├── assets                # static assets
├── data                  # objects w/ the text of the site for easy editing
├── styles                # css stylesheets
├── index.html            # each page has an index.html (the page template)
├── index.js              # and an index.js (the JS entry point w/ the preact)
└── README.md
```

